import { ViewStateMessagePipe } from './view-state-message.pipe';

describe('ViewStateMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewStateMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
